import { styled } from '@mui/system'

import Box from '@mui/material/Box'

const CustomBox = styled(Box)({
    width: '100%',
    height: '700px',
    padding: '5px 10px',
    boxSizing: 'border-box',
});

const SpritesCardListItemBody = () => {
  return (
    <CustomBox>
      TET
    </CustomBox>
  )
}

export default SpritesCardListItemBody
